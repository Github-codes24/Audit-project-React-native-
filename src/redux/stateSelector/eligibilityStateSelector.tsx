export const getIsEligibilityTestGiven = (state) =>
    state?.eligibiltyReducer?.isEligibilityTestGiven;

export const getEligibiltyResult=(state) =>
state?.eligibiltyReducer?.EligibilityResult;

   